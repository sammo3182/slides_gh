---
title: "Descriptive Statistics"
subtitle: "Large N & Leeuwenhoek (70700173)"
author: "Yue Hu"
institution: "Tsinghua University"
# date: "`r Sys.Date()`"
output:
  xaringan::moon_reader:
    self_contained: FALSE
    chakra: libs/remark-latest.min.js
    css: 
      - default
      - zh-CN_custom.css
      - style_ui.css
    mathjax: "https://cdn.bootcss.com/mathjax/2.7.1/MathJax.js?config=TeX-MML-AM_HTMLorMML"
    nature:
      highlightStyle: github
      highlightLines: true
      highlightSpans: true
      countIncrementalSlides: false
      ratio: 16:9
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE, htmltools.dir.version = FALSE)

library(pacman)
p_load(icons, xaringanExtra)

use_xaringan_extra(c("tile_view", # O
                                    "broadcast", 
                                    "panelset",
                                    "tachyons"))
use_extra_styles(
  hover_code_line = TRUE,         #<<
  mute_unhighlighted_code = FALSE  #<<
)

use_fit_screen()

# Functions preload
set.seed(313)
```

## Overview

+ Nature of data: population vs. sample &rarr; randomization   

+ Describing data:  variable &rarr; measurement

---

class: inverse, bottom

# Nature of Data

???

When you call the thing in your hand is a dataset, what does that mean?

---

## Etymology of Data

~~The quantities, characters, or symbols on which operations are performed by a computer, being stored and transmitted in the form of electrical signals and recorded on magnetic, optical, or mechanical recording media.~~

.bg-black.golden.ba.shadow-5.ph4.mt3[

Things known or assumed as facts, making the basis of reasoning or calculation.

.tr[
---[Oxford Language](https://languages.oup.com/google-dictionary-en/)
]
]

--

Origin:

.center[<img src = "images/desc_etymData.png" height = 150>]

---

## Data of Who

.pull-left[

### Population

1. Population is specific
1. Collection
1. Abstract
]

???

1. Population is not everything but a finite or infinite collection of items **under consideration**.
1. It's all its kinds;
1. Not only human being, but any observation units

--

.pull-right[

### Sample

1. Relative 
1. Many ways to get
    + Data generation process (*DGP*!!)
1. Concrete
]

???

1. Relative to population
1. Important concept to evaluate validity
    + The data collection process (over time);
    + A specific statistical model in a simulation
1. You know who are in the sample

--

.center[
### But why? 
1. Limited resources
1. Scarcity (only one sample can be used)
1. Destructive testing
1. More accurate
]

???

1. No money
1. Confucius institute
1. Finding some parts' characteristics
1. Text data

---

## Population .large[&#8799;] Sample

.center[
RANDOM Sampling 

<img src = "images/desc_onePunchMan.gif" height = 300>
]

Why amazing?

Confounder-free (?) + Representativeness (?)

???

Choose by chance regardless of anything else

埼玉

Not necessarily confounder free or representative, depending on the size

---

## How To
.center[
## Simple Randomization

<img src = "images/desc_random.gif" height = 200>
]

???

In a simple random sampling, there's a chance that you drawing all the rich out.

e.g., 

1. Checking the food in Tsinghua: All dishes from Qingfen 3rd floor; 
1. Checking stealing takeout: All thieves 

--

.pull-left[
## Block Randomization

1. Blocks (equally representative)
1. Randomly drawing blocks
1. Everyone of the block in
]

???

e.g., 

Study Tsinghua students

1. Dorm/Classroom
1. Drawing classrooms


--

.pull-right[
## Stratified Randomization

1. Variable for stratify
1. Randomly yet representative drawing from each stratum

]

???

Study Tsinghua students

1. Male/female
1. Liberal-arts/humanity/science/engineering

---

class: inverse, bottom

# Describing Data

???

How can you describe the data in your hand

---

## Variable from a View of Data

.pull-left[

*Specially*,

A variable = a dimension = a column

*Empirically*,

A variable = a vector
= a piece of data

*Relationally*,

An observation &sub; {var1, var2, var3,...}

]
.pull-right[<img src = "images/desc_multidimension.jpg" height = 500 width = 600>]

---

class: center

## Etymology of Variable

<img src = "images/desc_etymVariable.png" height = 400>

* Constant vs. variables
* Dependent/outcome/response ~
* Independent/explanatory/treatment ~

---

class: middle, center, large

 Q: What research may ask a variable to be .red[constant]?

???

Testing if X is a necessary condition for Y

e.g., 

Is hard work necessary for successes?

1. A works 12h, B works 12h, A success, B doesn't

Is good governance produced by democracy?

1. Democracy with Trump
1. Democracy with Clinton

---

## Types of Variable

+ Nominal/categorical
+ Ordinal

--

+ Interval: Meaningful distance
    + Discrete/continuous
    + Zero is a terminal
    + Meaningful addition/subtraction
+ Ratio: Interval with meaningful zero
    + Zero means absence
    + Meaningful multiplication/division

???

feeling thermometer: 0-200，冷淡到热情, 0是非常极端的情绪，不代表没有, so an interval;
-100-0-100， 0代表0的意思，ratio 是唯一一种scale可以表示meaningful ratio

Ratio: the ratio of two points on the scale conveys the same meaning regardless of where one is on the scale
e.g.1, Public budget, safty:health = 4:1 then no matter how much in total, it will 4:1
e.g.2, event history


---

## Features of Variable

Level of measurement

+ Raw data
+ Frequency distribution: 
  n<sub>i</sub>, n<sub>.</sub> = &sum;n<sub>i</sub>, n<sub>..</sub> = &sum;n<sub>ij</sub>

--

+ Histogram
+ Time series plots, scatter plots

--

+ Percentiles
+ Moments

???

Information lost increasingly more

---

## Describing a Variable with a Number

+ .red[Mode]: most frequent occurring number, not good of central tendency, bimodal (two modes)

--

+ .red[Median]: at 50%
    + Odd: middle value
    + Even: mean of two mid values
    + Sensitive for outliers
    
--

+ .red[Mean]: $\bar{X}\rightarrow\mu$
    + Group: $\bar{X} = 1/n\sum xf$, x is the cell midpoint and f is the frequency of $X_i$.
    + Symmetric distribution, median and mean are identical

---

class: middle, center

```{r, fig.show='animate', animation.hook = 'gifski', fig.width=6, fig.height=2, echo=FALSE, message=FALSE, warning=FALSE, results = 'hide', interval=0.5, eval = FALSE, include=FALSE}
library(fGarch)
library(ggplot2)
library(gifski)
skew <- seq(0.5, 1, 0.05)
skew2 <- seq(1.1, 2, 0.1)
skew3 <- seq(1.9, 1, -0.1)
skew4 <- seq(0.95, 0.55, -0.05)
skew <- c(skew, skew2, skew3, skew4)
plot1 <- function(x){
	d <- lapply(1:40, function(x){
		d <- data.frame(x=rsnorm(100000, mean=0, sd=2, xi=skew[x]), frame=x)
		return(d)
	})
medians <- c(seq(0.31,-0.31, -0.031), seq(-0.279,0.279, 0.031))
#medians <<- lapply(1:40, function(x) median(d[[x]]$x))
p <- lapply(1:40, function(y) ggplot(data=d[[y]], aes(x))+geom_histogram(binwidth=0.25, color="white", fill="skyblue2")+theme_light()+theme(panel.border=element_blank(),panel.grid.minor=element_blank(), axis.ticks=element_blank(), axis.text=element_blank())+guides(fill=FALSE)+labs(x=NULL, y=NULL)+scale_y_continuous(expand=c(0,0),limits=c(0,5600), breaks=c(0,2000, 4000))+geom_vline(xintercept=0, size=0.5, linetype="dashed")+geom_vline(xintercept=medians[y], size=0.5)+xlim(-5,5)+annotate("text", label="Mean", size=3.4, x=-4.1, y=5300, hjust=0)+annotate("text", label="Median", size=3.4 ,x=-4.1, y=4600, hjust=0)+geom_segment(aes(x=-4.8, xend=-4.3, y=5300, yend=5300), linetype="dashed")+geom_segment(aes(x=-4.8, xend=-4.3, y=4600, yend=4600)))
print (p)
}

gif_file <-  file.path(getwd(), 'median.gif')
save_gif(plot1(), gif_file= gif_file, progress = FALSE, loop= TRUE, delay= 0.5, width=400, height= 133, res= 100)

utils::browseURL(gif_file)
```

<img src="images/median.gif" height = 400 />

---

## Describing the Dispersion of a Variable

Range: Max - min; [min, max]

Q: What's the range of a constant, e.g., 2, 2, 2, 2

--

.center[<img src="images/desc_dispersion.png" height = 400 />]

---

+ Interquartile Range (IQR): [25% quartile, 75% quartile]
+ Mean absolute deviation (MAD): $1/n\sum|X - \bar{X}|$
+ Mean square deviation (MSD): $1/n\sum(X - \bar{X})^2$

???

MAD: Intractable, difficult in differentiate absolute values

--

&#8618; Variance: &sigma;<sup>2</sup> for the population; s<sup>2</sup> for a sample

\begin{align}
\sigma^2 =& \sum(X - \mu)^2p(X)\\
s^2 =& \sum(X - \bar X)^2p(x) \\
=& \frac{\sum(X - \bar{X})^2}{n - 1}.
\end{align}

???

&sigma;: sigma

---

## Why n-1? 

### Intuitively

Only when there are .red[more than one] item, the variance can exist, therefore the d.f. is $n - 1$.

--

### Mathematically

Degree of freedom (d.f.)<sup>\*</sup>

$$\left[\begin{array} {ccc}
		X_1\\
		\vdots \\
		X_n
		\end{array}\right] = \bar{X}\left[\begin{array} {rrr}
		1 \\
		\vdots \\
		1
		\end{array}\right] + \left[\begin{array} {rrr}
		X_1 - \bar{X} \\
		\vdots \\
		X_n - \bar{X}
		\end{array}\right]
		$$

--

.footnote[
\* For a crosstable: $(r - 1)(c - 1)$
]

???

The first vector on the right-hand side is constrained to be a multiple of the vector of 1's, and the only free quantity is ${\bar {X}}$. It therefore has 1 degree of freedom.

The second vector is constrained by the relation ${\displaystyle \sum _{i=1}^{n}(X_{i}-{\bar {X}})=0}$. The first n − 1 components of this vector can be anything. However, once you know the first n − 1 components, the constraint tells you the value of the nth component. Therefore, this vector has n − 1 degrees of freedom.

In statistical testing applications, often one isn't directly interested in the component vectors, but rather in their squared lengths.

---

&sigma;<sup>2</sup> is often written in another format:

Given &mu; = &sum;Xp(x), &sum;p(x) = 1,<sup>\*</sup>

\begin{aligned}
\sigma^2 =& \sum(X - \mu)^2p(x),\\
=& \sum(X^2 -2X\mu + \mu^2)p(x),\\
=& \sum X^2p(x) - 2\mu\sum Xp(x) + \mu^2\sum p(x). \\
=& \sum X^2p(x) - \mu^2.
\end{aligned}
 
--

.footnote[NB: Pay attention to the red[punctuation marks].]
 
--

### Standard deviation (SD)

+ Population: $\sigma = \sqrt{\pi(1 - \pi)}$
+ Group: $\sqrt{f/n\sum(X - \bar{X})^2}$
+ Sample: s

---

## Wrap Up

.pull-left[
### Nature of data

Population/sample &rarr; randomization  

+ Population &rarr; sample
    + DGP
+ Randomization
  + Simple ~
  + Block ~
  + Stratefied ~
]

.pull-right[

### Describing data

Variable &rarr; measurement

+ Variable
+ Types: Level of Measurements
    + Nominal < Ordinal < Continuous (Interval/ratio)
+ Describing a Variable
    + Average (mode, median, mean)
        + Degree of freedom
    + Dispersion (range, IQR, MAD/MSD) &rarr; Variance
        + Standard deviation
]

---

class: center, middle, large

[Let's do some math!](../mathCamp/mathcamp.html)

```{r pdfPrinting, eval = FALSE, include = FALSE}
pagedown::chrome_print(list.files(pattern = "02_.*.html"), 
                       timeout = 500, 
                       box_model = "padding")
```